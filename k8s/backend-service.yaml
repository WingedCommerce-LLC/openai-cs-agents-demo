apiVersion: v1
kind: Service
metadata:
  name: agents-backend-service
  namespace: openai-agents
  labels:
    app: agents-backend
spec:
  selector:
    app: agents-backend
  ports:
    - port: 80
      targetPort: 8000
      name: http
  type: ClusterIP
